<table>
  <tr>
    <th>User Name</th>
    <th>Show Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @subscribes.each do |subscribe| %>
  <tr>
    <td><%= subscribe.user ? (link_to subscribe.user.fname, show_path(subscribe.user)) : "" %></td>
    <td><%= subscribe.show ? (link_to subscribe.show.name, show_path(subscribe.show)) : "" %></td>
    <td><%= link_to 'Show', subscribe %></td>
    <td><%= link_to 'Edit', edit_subscribe_path(subscribe) %></td>
    <td><%= link_to 'Destroy', subscribe, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Subscribe', new_subscribe_path %>